import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1 },
  sloganContainer: { flex: 1 },
  progressContainer: { flex: 1 },
  doItContainer: { flex: 1 },
});

export default styles;
